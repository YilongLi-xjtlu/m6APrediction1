% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{prediction_single}
\alias{prediction_single}
\title{Predict m6A status for a single sequence}
\usage{
prediction_single(
  ml_fit,
  gc_content,
  RNA_type,
  RNA_region,
  exon_length,
  distance_to_junction,
  evolutionary_conservation,
  DNA_5mer,
  positive_threshold = 0.5
)
}
\arguments{
\item{ml_fit}{A fitted \code{randomForest} classification model
for m6A prediction.}

\item{gc_content}{Numeric GC content of the transcript region.}

\item{RNA_type}{Character or factor giving the transcript type
(e.g. \code{"mRNA"}, \code{"lincRNA"}, \code{"lncRNA"},
\code{"pseudogene"}).}

\item{RNA_region}{Character or factor describing the region
(e.g. \code{"CDS"}, \code{"intron"}, \code{"3'UTR"},
\code{"5'UTR"}).}

\item{exon_length}{Numeric exon length.}

\item{distance_to_junction}{Numeric distance to the nearest
exon junction.}

\item{evolutionary_conservation}{Numeric conservation score.}

\item{DNA_5mer}{Character string containing the 5-mer centred
on the candidate m6A site.}

\item{positive_threshold}{Numeric decision threshold on the
Positive-class probability. Defaults to 0.5.}
}
\value{
A named vector of length two with elements
\code{predicted_m6A_prob} and \code{predicted_m6A_status}.
}
\description{
This convenience wrapper creates a one-row feature data frame
from user-supplied covariates and calls
\code{\link{prediction_multiple}} to obtain a probability and a
Positive/Negative label for one candidate site.
}
\examples{
\dontrun{
  rf_model <- readRDS(system.file("extdata", "rf_fit.rds",
                                  package = "m6APrediction"))
  prediction_single(
    rf_model,
    gc_content = 0.55,
    RNA_type   = "mRNA",
    RNA_region = "CDS",
    exon_length          = 2200,
    distance_to_junction = 150,
    evolutionary_conservation = 0.8,
    DNA_5mer = "ATGCG",
    positive_threshold = 0.5
  )
}

}
